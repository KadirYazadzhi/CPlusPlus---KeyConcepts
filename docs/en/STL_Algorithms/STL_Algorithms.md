# STL Algorithms in C++

## 1. Introduction

Algorithms in `<algorithm>` work on iterators.

---

## 2. Examples

```cpp
std::sort(v.begin(), v.end());
std::find(v.begin(), v.end(), 5);
std::for_each(v.begin(), v.end(), func);
```

---

## 3. Summary

Prefer over raw loops.
